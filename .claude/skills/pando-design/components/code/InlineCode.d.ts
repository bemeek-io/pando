import * as React from 'react';

/** Inline code on a vegetation tint: 2px radius, 1px 4px padding. */
export interface InlineCodeProps extends React.HTMLAttributes<HTMLElement> {
  children?: React.ReactNode;
}

export declare function InlineCode(props: InlineCodeProps): JSX.Element;
