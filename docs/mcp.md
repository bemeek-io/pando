<!-- Generated by cmd/gen-reference from the running code. Do not edit by hand;
     run `make reference`. `make check` fails when this file is out of date. -->

# Pando over MCP

An agent holds a token and is therefore a principal like any other: it acts as
its owner, is bounded by their live grants, and its actions land in the audit log
under their name (R-262).

## Connecting

`pando mcp` speaks MCP over stdio. In Claude Code:

```
claude mcp add pando -- pando mcp
```

Or, by hand, in an MCP client's configuration:

```json
{
  "mcpServers": {
    "pando": {
      "command": "pando",
      "args": ["mcp"],
      "env": {
        "PANDO_SERVER": "https://pando.example.com",
        "PANDO_TOKEN": "tok_…"
      }
    }
  }
}
```

## Tools

One tool per endpoint, deliberately. A tool that composed several calls would be
a capability the CLI and console do not have. Exec, reading a secret's value,
changing grants or policy, and deleting a user are not offered: host policy is the
boundary (O-12), and offering a tool policy will refuse wastes the agent's turn.

| Tool | What it does | Arguments |
| --- | --- | --- |
| `pando_list_apps` | List the apps you can manage, with their current state. | none |
| `pando_get_app` | Get one app: its name, source, state and pinned spec. | `app_id` |
| `pando_create_app` | Create an app from a git repository. Returns immediately with the app in draft while Pando works out how to run it; call pando_get_detection next. | `name`, `ref` (optional), `source_url` |
| `pando_get_detection` | What Pando worked out about an app, including any questions it needs answered before it can deploy. The questions are written to be answerable by whatever wrote the app. | `app_id` |
| `pando_answer_detection` | Answer one of the questions from pando_get_detection. | `answer`, `app_id`, `key` |
| `pando_accept_proposal` | Accept what Pando worked out and pin it as the app's setup. This does not deploy — call pando_deploy after. | `app_id` |
| `pando_plan` | Show what a deploy would do, without doing it. Side-effect free, so it is safe to call after any change to check the change is deployable. | `app_id` |
| `pando_deploy` | Deploy an app. Returns once the deployment has been accepted, not once it is running. | `app_id`, `idempotency_key` (optional) |
| `pando_get_logs` | Read an app's recent logs. | `app_id` |
| `pando_get_status` | What an app is doing right now: running, degraded, failed, and why. | `app_id` |

